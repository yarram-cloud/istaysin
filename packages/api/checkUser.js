const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function main() { const user = await prisma.globalUser.findFirst({ where: { email: 'admin@istaysin.com' } }); console.log(user ? 'User exists' : 'User NOT found'); if (user) { console.log('IsActive:', user.isActive); console.log('Roles:', user.roles); } } main().finally(() => prisma.$disconnect());
