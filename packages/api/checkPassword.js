const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const user = await prisma.globalUser.findFirst({ where: { email: 'admin@istaysin.com' } }); if (user) { const isMatch = await bcrypt.compare('Admin@2026', user.passwordHash); console.log('Password match:', isMatch); } } main().finally(() => prisma.$disconnect());
