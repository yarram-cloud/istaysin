const { PrismaClient } = require('@prisma/client'); const bcrypt = require('bcryptjs'); const prisma = new PrismaClient(); async function main() { const hash = await bcrypt.hash('Admin@2026', 10); await prisma.globalUser.updateMany({ where: { email: 'admin@istaysin.com' }, data: { passwordHash: hash, failedLoginAttempts: 0, lockedUntil: null } }); console.log('Password reset successfully'); } main().finally(() => prisma.$disconnect());
